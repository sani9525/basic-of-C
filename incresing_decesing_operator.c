// ++ it's means one by one incesing operator 
// -- it's means one by one decresing operator 
#include <stdio.h>
int main ()
{
int a=0;
for (a=10; a>=0; a--) // a++ is when put a-- then its work decresing operator 
{
    printf("%d \n",a);
}

    return 0;
}